<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Список пользователей" Height="400" Width="600" ResizeMode="NoResize"
        Loaded="MainWindow_OnLoaded"
        KeyDown="MainWindow_OnKeyDown">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="295"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="320"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="UsersList" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" ColumnWidth="84" MinColumnWidth="84" ItemsSource="{Binding}"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  CanUserResizeColumns="False" SelectionMode="Single"
                  AutoGenerateColumns="False" HorizontalAlignment="Center">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Lastname}"/>
                <DataGridTextColumn Header="Инициалы" Binding="{Binding Initials}"/>
                <DataGridTextColumn Header="Должность" Binding="{Binding Position}"/>
            </DataGrid.Columns>
        </DataGrid>
        <WrapPanel x:Name="ButtonsPanel" Grid.Column="1" Grid.Row="1" Orientation="Horizontal" Margin="4 10 0 0">
            <Button x:Name="InsertButton" Width="80" Content="Добавить" Margin="0 0 20 0" Click="InsertButton_OnClick"/>
            <Button x:Name="DeleteButton" Width="80" Content="Удалить" Margin="0 0 20 0" Click="DeleteButton_OnClick"/>
            <Button x:Name="ChangeButton" Width="80" Content="Изменить" Margin="0 0 0 0"/>
        </WrapPanel>
    </Grid>
</Window>
